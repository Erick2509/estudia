(()=>{'use strict';const $=id=>document.getElementById(id);let signedIn=false;const links=[...document.querySelectorAll('#appNavbar .nav-link,#mobileNav a[data-nav]')];function route(){let id=(location.hash||'#inicio').slice(1);if(!$(id)||!['inicio','tecnicas','organizacion','estres','progreso','consejos','nosotros'].includes(id))id='inicio';document.querySelectorAll('#contenido>section').forEach(s=>{s.hidden=s.id!==id;s.classList.toggle('page-enter',s.id===id)});links.forEach(a=>{const active=a.getAttribute('href')==='#'+id;a.classList.toggle('active',active);if(active)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});const menu=$('mobileMoreMenu');if(menu)menu.hidden=true;$('mobileMore')?.setAttribute('aria-expanded','false');if(signedIn)window.scrollTo({top:0,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'instant':'smooth'});}window.addEventListener('hashchange',route);window.addEventListener('estudia:auth',e=>{signedIn=e.detail.signedIn;$('mobileNav').hidden=!signedIn;if(e.detail.name){const first=e.detail.name.split(/[ @]/)[0];$('homeGreeting').textContent='¡Hola, '+first+'! 👋';}route()});window.addEventListener('estudia:data',e=>{const {tareas=[],practicas=[]}=e.detail;const done=tareas.filter(t=>t.completada).length;$('homePending').textContent=String(tareas.length-done);$('homeDone').textContent=String(done);$('homePractices').textContent=String(practicas.length)});const date=new Intl.DateTimeFormat('es-PE',{day:'numeric',month:'long'}).format(new Date());$('homeDate').textContent=date;const more=$('mobileMore'),menu=$('mobileMoreMenu');more?.addEventListener('click',()=>{menu.hidden=!menu.hidden;more.setAttribute('aria-expanded',String(!menu.hidden))});$('mobileLogout')?.addEventListener('click',()=>$('salir').click());route();})();